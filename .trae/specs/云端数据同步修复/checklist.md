- [x] 本地数据库使用新库名 `AIFlashCardsDB_v2`，schema 主键统一为 `&id`
- [x] `ensureDbReady()` 通过原生 IndexedDB 读取旧库，避免 Dexie 主键变更检测
- [x] 旧库数据成功迁移到新库（categories/units/cards/cardStatus/bookmarks/wrongAnswers）
- [x] 迁移失败时走"删除旧库 + 删除新库 + 重建"的兜底逻辑，不影响应用启动
- [x] AppContext `loadCategories` 在数据库操作前调用 `ensureDbReady()`
- [x] `snakeToCamel` 无条件兼容 `title` → `name` 字段映射
- [x] `camelToSnake` 按 `TABLE_SCHEMAS[tableName].fields` 过滤输出字段
- [x] `camelToSnake` 正确处理时间戳转换（number → ISO string），且会补充 `created_at`/`updated_at`
- [x] 所有 `snakeToCamel` 调用均传入 tableName 参数
- [x] `addCloudRecord` 使用 `camelToSnake`（而非内联正则）转换 payload 字段
- [x] `getCloudTableData` 仅在 schema 含 `created_at` 时才按该字段排序
- [x] `fetchAllPages` 在 `created_at` 排序失败时回退到无序查询
- [x] CloudDataDetail.jsx `TABLE_META` 含 `wrong_answers`
- [x] CloudDataDetail.jsx `FORM_FIELDS` 含 `wrong_answers`
- [x] CloudDataDetail.jsx `LOCAL_TABLE_MAP` 含 `wrong_answers: 'wrongAnswers'`
- [x] CloudDataDetail.jsx `handleDownload` 对 `title`→`name`、时间戳做归一化
- [x] 首页添加/编辑/删除分类功能正常
- [x] 控制台无 `UpgradeError: Not yet support for changing primary key` 错误
- [x] 文档分布.txt 已同步更新数据库/同步相关说明
