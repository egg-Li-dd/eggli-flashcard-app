# 艾宾浩斯背诵计划系统 - 详细应用报告

**文档版本**: v1.0  
**生成日期**: 2026-06-16  
**适用范围**: AI Flashcard App 背诵计划功能

---

## 目录

1. **系统概述** - 什么是艾宾浩斯？为什么要用它？
2. **核心算法 SM-2** - 数学公式与完整规则说明
3. **延迟复习惩罚机制** - 没有按时复习该怎么办？
4. **数据存储结构** - 数据库表设计与字段说明
5. **前端界面流程** - 用户看到什么，点击哪里
6. **背诵模式隔离** - 5种模式互不干扰的设计
7. **未来预测系统** - 智能预测未来7天/30天复习量
8. **记忆阶段划分** - 从「未学习」到「长期记忆」
9. **测试评分与艾宾浩斯的联动** - 答错了怎么办？
10. **云端同步策略** - 换设备也能继续复习
11. **关键代码文件索引** - 开发者快速定位入口

---

## 1. 系统概述

### 1.1 什么是艾宾浩斯遗忘曲线？

德国心理学家赫尔曼·艾宾浩斯在1885年发现了人类记忆的遗忘规律。遗忘曲线表明：
- 学习后的第1天，记忆力保留约33%
- 第2天，保留约28%
- 第6天，保留约25%
- 31天后，保留约21%

**核心发现**: 如果在即将遗忘的时候进行复习，可以用最少的时间获得最佳的记忆效果。

### 1.2 本App如何应用艾宾浩斯？

我们使用**SM-2算法**（Anki等主流记忆软件的核心算法），它基于艾宾浩斯理论，但通过用户的实际复习反馈动态调整复习间隔。

```
用户学习卡片 → 标记「已掌握」 → 系统计算下次复习时间
     ↑                       ↓
     └────── N 天后再次复习 ←──┘
```

### 1.3 系统架构图

```
┌──────────────────────────────────────────────────────────────────┐
│                       背诵计划页面                               │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │ 顶部：同步状态指示器 + 返回按钮 + 标题                    │ │
│  ├─────────────────────────────────────────────────────────────┤ │
│  │ 主 Tab：【今日计划】 【未来预测】 【学习分析】       │ │
│  ├─────────────────────────────────────────────────────────────┤ │
│  │ 模式 Tab：【总览】【艾宾浩斯】【进度追踪】【薄弱】│ │
│  └─────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────┘
```

---

## 2. 核心算法 SM-2

### 2.1 三个核心参数

每张卡片都维护这三个值：

| 参数 | 含义 | 初始值 | 变化范围 |
|-----|------|--------|---------|
| `repetitions` | 连续成功复习次数 | 0 | 0, 1, 2, 3, ... |
| `interval` | 当前复习间隔（天） | 1 | 1, 6, 16, 44, ... |
| `easeFactor` | 难度系数 | 2.5 | 1.3 ~ 3.0 |

### 2.2 SM-2 标准公式（按时复习的情况下）

当用户在计划的日期或之前标记「已掌握」时：

```
repetitions = repetitions + 1

if repetitions == 1:
    interval = 1          # 第1次成功 → 1天后复习
elif repetitions == 2:
    interval = 6          # 第2次成功 → 6天后复习
else:
    interval = ceil(prevInterval × easeFactor)   # 后续按系数延长

easeFactor = max(1.3, easeFactor + 0.1)   # 成功使难度系数增加
nextReviewAt = now + interval × 86400000    # 下次复习时间
```

**示例路径**（假设 easeFactor 始终为 2.5）：
```
第1次成功 → interval = 1 天
第2次成功 → interval = 6 天
第3次成功 → interval = ceil(6 × 2.5) = 15 天
第4次成功 → interval = ceil(15 × 2.5) = 38 天
第5次成功 → interval = ceil(38 × 2.5) = 95 天
...间隔越来越长
```

这意味着，一张卡片经过 **5次成功复习** 后，它的复习间隔已经接近100天——我们认为它已进入长期记忆。

### 2.3 失败时的处理（标记「待掌握」）

当用户标记「待掌握」，无论之前复习了多少次：

```
repetitions = 0           # 失败回退，重新开始积累
interval = max(1, ceil(prevInterval × 0.5))  # 间隔减半
easeFactor = max(1.3, easeFactor - 0.2)     # 难度系数降低
nextReviewAt = now       # 立即可复习（今天就需要再次复习）
```

---

## 3. 延迟复习惩罚机制

### 3.1 为什么需要惩罚？

如果用户计划10天后复习某卡片，但他30天后才打开App，这张卡片已经被遗忘了。如果还按原有的公式延长间隔，会导致学习效果下降。

因此，我们设计了**4级延迟惩罚**系统。

### 3.2 延迟等级判定

```
delayDays = floor( (当前时间 - nextReviewAt) / 86400000 )

delayDays <= 0         → 按时 / onTime
0 < delayDays <= 3     → 轻微延迟 / slight
3 < delayDays <= interval → 中等延迟 / medium
delayDays > interval   → 严重延迟 / severe
```

### 3.3 惩罚规则表（标记「已掌握」时）

| 延迟等级 | 延迟条件 | 对 reps 的影响 | 对 interval 的影响 | 对 easeFactor 的影响 |
|---------|---------|---------------|-------------------|----------------------|
| **按时 onTime** | delayDays ≤ 0 | reps + 1 | interval × easeFactor（正常） | easeFactor + 0.1 |
| **轻微 slight** | 0 < delayDays ≤ 3 | reps + 1 | min(计算值, prev + 3) | 不增加 |
| **中等 medium** | 3 < delayDays ≤ interval | reps 退 1，最小1 | ceil(prev × 0.6) | easeFactor - 0.1 |
| **严重 severe** | delayDays > interval | reps = 1，从新开始 | interval = 1 天 | easeFactor - 0.15 |

**示例：** 卡片已成功复习3次（reps=3, interval=15天, easeFactor=2.7），nextReviewAt 是2026-06-10。

| 今天日期 | delayDays | 等级 | 标记已掌握后的结果 |
|---------|-----------|------|-------------------|
| 2026-06-08 | -2 | onTime | reps=4, interval=41, easeFactor=2.8 |
| 2026-06-12 | +2 | slight | reps=4, interval=min(41,18)=18 |
| 2026-06-18 | +8 | medium | reps=2, interval=ceil(15×0.6)=9, easeFactor=2.6 |
| 2026-07-01 | +21 | severe | reps=1, interval=1, easeFactor=2.55 |

### 3.4 失败时的统一处理（不考虑延迟）

无论延迟程度如何，标记「待掌握」后的处理都是一样的：
```
reps = 0
interval = max(1, ceil(prev × 0.5))
easeFactor = max(1.3, prev - 0.2)
nextReviewAt = now   # 立即可复习
```

---

## 4. 数据存储结构

### 4.1 主要数据表

#### 表1: cardStatus（卡片学习状态）

**位置**: `IndexedDB -> db('AIFlashcardsDB_v2').cardStatus`

| 字段 | 类型 | 含义 | 艾宾浩斯用途 |
|-----|------|------|-------------|
| `id` | string | 记录ID | |
| `cardId` | string | 卡片ID | 关联卡片 |
| `categoryId` | string | 分类ID | 关联分类 |
| `status` | string | mastered/review | 手动状态标记 |
| `repetitions` | number | 连续成功次数 | **核心参数** |
| `interval` | number | 当前复习间隔（天） | **核心参数** |
| `easeFactor` | number | 难度系数（1.3-3.0） | **核心参数** |
| `nextReviewAt` | number | 下次复习时间戳（ms） | **决定是否到期** |
| `lastReviewedAt` | number | 上次复习时间戳 | 统计用 |
| `reviewCount` | number | 总复习次数 | 统计用 |
| `mode` | string | 操作来源模式 | **用于模式隔离** |
| `updatedAt` | number | 最后更新时间 | |

#### 表2: reviewHistory（复习历史记录）

**位置**: `IndexedDB -> db('AIFlashcardsDB_v2').reviewHistory`

| 字段 | 类型 | 含义 |
|-----|------|------|
| `id` | string | 记录ID |
| `cardId` | string | 卡片ID |
| `categoryId` | string | 分类ID |
| `wasMastered` | boolean | true=已掌握, false=待掌握 |
| `reviewedAt` | number | 复习时间戳 |
| `mode` | string | ebbinghaus/sequential/active/weak/test |

**用途**:
- 计算连续学习天数（streak）
- 历史分析（某卡片在测试中答错了多少次）
- 用户查看单张卡片的复习轨迹
- 云端同步时作为完整历史备份

#### 表3: studyPlans（学习计划配置）

**位置**: `IndexedDB -> db('AIFlashcardsDB_v2').studyPlans`

| 字段 | 类型 | 含义 | 默认值 |
|-----|------|------|-------|
| `id` | string | 记录ID | |
| `categoryId` | string | 分类ID | |
| `dailyReviewLimit` | number | 每日复习上限 | 50 张 |
| `dailyNewLimit` | number | 每日新学上限 | 20 张 |
| `priority` | string | low/medium/high | medium |
| `createdAt` | number | 创建时间 | |
| `updatedAt` | number | 更新时间 | |

**用途**:
- 未来7天预测中，超过 dailyReviewLimit 的日期会标红警告
- 用户可针对不同分类设置不同的学习节奏

### 4.2 数据库版本演变

```
v1: 初始卡片数据
v2: 新增 knowledge_point 字段
v3: 新增单元检测相关表
...
v6: 新增 reviewHistory、studyPlans 表
    为 cardStatus 表新增 mode 字段
```

**版本v6升级**（所有用户自动触发）:
- 旧数据会自动迁移到新表结构
- 已有 status 的卡片，如果没有 repetitions/interval/easeFactor，按默认值初始化
- 新卡片第一次复习时才创建 cardStatus 记录

---

## 5. 前端界面流程

### 5.1 入口1：背诵页按钮 → 计划页

**路径**: `/memorize` → `/memorize/plan`

```
背诵页面
├── 顶部左侧：【☰】菜单
├── 右侧按钮：【立即复习】
└── 点击立即复习 → navigate('/memorize/plan?entryType=review')

背诵页面
├── 右侧边栏按钮
└── 点击 → navigate('/memorize/plan?entryType=sidebar')
```

### 5.2 入口2：从计划页跳回背诵页

**路径**: `/memorize/plan` → `/memorize?categoryId=xxx&mode=ebbinghaus&fromPlan=true`

```
计划页 [今日计划 Tab]
├── 艾宾浩斯模式汇总卡片
└── 点击「进入复习」 → navigate('/memorize?categoryId=xxx&mode=ebbinghaus&fromPlan=true')

背诵页面收到参数后自动：
1. 选中指定分类
2. 切换到 ebbinghaus 模式
3. 只显示到期卡片
4. 完成后自动返回计划页查看进度
```

### 5.3 三个主视图详解

#### 5.3.1 今日计划视图 (Today View)

```
┌────────────────────────────────────────────────────┐
│ 今日任务                                             │
│  ┌──────────────┐  到期：27张卡片                    │
│  │              │  已安排复习但未到期：34张          │
│  │  总览统计    │  未学习：15张                      │
│  │              │  已掌握：68张                      │
│  └──────────────┘                                     │
│                                                         │
│ 模式汇总卡片（横向排列）                               │
│ ┌────────────┐ ┌────────────┐ ┌────────────┐      │
│ │🔵 艾宾浩斯 │ │ 📊 进度追踪 │ │💪 薄弱卡片│      │
│ │ 27 张到期  │ │ 分类进度条 │ │ 8张待掌握 │      │
│ │ 已同步     │ │ 仅本地     │ │ 仅本地     │      │
│ └────────────┘ └────────────┘ └────────────┘      │
│                                                         │
│ 分类卡片列表                                           │
│ ┌─────────────────────────────────────────────────┐   │
│ │ 📂 分类A (35张)           🔵 已同步             │   │
│ │ ┌进度条 68% ───────────────> 已掌握24张         │   │
│ │ 📅 到期：10张  新学建议：5张                    │   │
│ │ [进入复习]                                       │   │
│ └─────────────────────────────────────────────────┘   │
│                                                         │
│ ┌─────────────────────────────────────────────────┐   │
│ │ 📂 分类B (28张)           ⚪ 仅本地             │   │
│ │ ┌进度条 35% ─────> 已掌握10张                    │   │
│ │ 📅 到期：8张  新学建议：5张                     │   │
│ │ [进入复习]                                       │   │
│ └─────────────────────────────────────────────────┘   │
└────────────────────────────────────────────────────┘
```

#### 5.3.2 未来预测视图 (Future View)

```
┌────────────────────────────────────────────────────┐
│ 未来7天到期卡片数                                    │
│                                                      │
│   柱状图                                              │
│    │╲                                                  │
│    │ ╲  柱子高度 = 当天到期卡片数                    │
│    │  ╲                                                │
│    │█  │█  │█  │█  │█  │█  │█                       │
│   Today +1   +2   +3   +4   +5   +6                  │
│   27    14   18   32   22   15   19                  │
│        ↑ 超过每日上限50则标红警告                     │
│                                                      │
│ 备注：顺序/活跃/薄弱模式不支持长期预测              │
│       这些模式的数据仅保存在本地                      │
└────────────────────────────────────────────────────┘
```

**预测算法** (函数 `predictFutureDueCards`):

```
for each day in 未来7天:
    count = 0
    for each card:
        if card.nextReviewAt 在该天范围内:
            count++
    result.push({ date, count })
```

#### 5.3.3 学习分析视图 (Analysis View)

```
┌────────────────────────────────────────────────────┐
│ 掌握进度分析                                          │
│                                                      │
│ 环形图（记忆阶段分布）                                │
│        ┌────┐                                        │
│       ╱ 长期 ╲  28% (68张)                          │
│      │ 记忆  │ 🟢 #6BCB77                            │
│     ╱ ╲    ╱ ╲                                      │
│    │  中期 ╱   ╲  15% (37张)                        │
│     ╲ 记忆 ╲  ╱   🟡 #FFD93D                        │
│      ╲    ╲╱                                          │
│       ╲  ╱   到期复习 12% (29张) 🔴 #FF6B6B         │
│        ╲╱                                             │
│       ╱╲   短期记忆 18% (44张) 🟠 #FFA94D          │
│      ╱  ╲                                            │
│     ╱ ╲  ╲   未学习 27% (65张) ⚪ #C0C0C0          │
│    │ ╲  ╱                                           │
│     ╲╱                                                │
│      └──┘                                            │
│                                                      │
│ 阶段说明:                                            │
│ ├ due: nextReviewAt <= now → 需要今天复习           │
│ ├ short: reps=1~2 → 短期记忆                        │
│ ├ medium: reps=3~4 → 中期记忆                       │
│ ├ long: reps>=5 → 长期记忆                          │
│ ├ unstarted: 无记录 → 还没学习                      │
│ └ regressed: reps=0 → 失败回退需重新开始             │
│                                                      │
│ 分类进度表:                                          │
│ ┌───────────────────────────────────────┐           │
│ │ 分类A           68% ████████▍       │           │
│ │ 分类B           35% ████▌             │           │
│ │ 分类C           82% █████████▎       │           │
│ └───────────────────────────────────────┘           │
│                                                      │
│ [查看长期记忆卡片] → /stats/longterm                │
│ （显示所有reps>=5的卡片详情）                        │
└────────────────────────────────────────────────────┘
```

---

## 6. 背诵模式隔离系统

### 6.1 五种背诵模式

| 模式 | mode 值 | 是否修改 SM-2 参数 | 是否同步云端 | 说明 |
|-----|---------|-------------------|------------|------|
| **艾宾浩斯** | ebbinghaus | ✅ 是 | ✅ 是 | 基于SM-2算法的间隔重复复习 |
| **顺序浏览** | sequential | ❌ 否，仅更新 status | ❌ 否 | 按卡片顺序浏览 |
| **活跃学习** | active | ❌ 否，仅更新 status | ❌ 否 | 打乱顺序快速复习 |
| **薄弱卡片** | weak | ⚠️ 缩短 interval，reps不变 | ❌ 否 | 专注待掌握卡片复习 |
| **测试评分** | test | ❌ 否，仅更新 status | ✅ 是（history同步） | 单元/分类检测后评分 |

### 6.2 为什么要隔离？

**问题**: 如果用户在顺序模式下标记了100张卡片为「已掌握」，这些卡片的SM-2参数会被错误地修改——但用户并没有真正按间隔复习。

**解决方案**: 在 `setCardStatus` 函数中，根据传入的 `mode` 参数执行不同的更新策略：

```javascript
function setCardStatus(cardId, categoryId, status, opts) {
    const { mode = 'sequential' } = opts

    switch mode:
        case 'ebbinghaus':
            // 完整SM-2算法（含延迟惩罚）
            return applySM2WithDelay(record, wasMastered)

        case 'sequential':
        case 'active':
            // 只更新 status 和 reviewCount
            // 不修改 repetitions/interval/easeFactor/nextReviewAt
            return { ...record, status, reviewCount: record.reviewCount + 1 }

        case 'weak':
            // 缩短 interval + 降低 easeFactor
            // reps 不变（不会因为薄弱复习而"进步"）
            // 使卡片更快再次出现
            return {
                ...record,
                interval: ceil(record.interval × 0.8),
                easeFactor: min(3.0, record.easeFactor + 0.05),
                repetitions: record.repetitions,  // 保持不变！
                nextReviewAt: now + newInterval × DAY_MS
            }

        case 'test':
            // 测试评分只是抽样验证，不改变长期计划
            // 只更新 status 和 reviewCount
            return { ...record, status, reviewCount: record.reviewCount + 1 }

    // 写入reviewHistory（所有模式都写，history用于分析）
    addReviewHistory({ cardId, categoryId, wasMastered, mode })
}
```

### 6.3 薄弱模式的特殊设计

薄弱模式的目标是**强化复习**，所以当用户在薄弱模式下标记「已掌握」时：
- interval 缩短到原有的 80% → 卡片会更早再次出现
- easeFactor 微增 0.05 → 对用户的掌握给予有限的正面反馈
- reps 保持不变 → 不会因为"在薄弱模式下复习了"就让卡片跳过几个阶段

这是一种「稳健保守」的策略：**让薄弱卡片保持在可被复习的范围内，直到用户在艾宾浩斯的正常节奏中证明了自己的掌握程度。**

---

## 7. 未来预测系统

### 7.1 预测未来N天到期卡片数

**函数**: `predictFutureDueCards(cards, statusesByCard, daysAhead=7, now)`

**算法**:
```
for i from 0 to daysAhead - 1:
    dayStart = 今天00:00 + i × 86400000ms
    dayEnd = dayStart + 86400000ms
    count = 0
    for card in cards:
        status = statusesByCard.get(card.id)
        // 无记录的新卡片视为今天到期
        nextReviewAt = status?.nextReviewAt || dayStart
        if dayStart <= nextReviewAt < dayEnd:
            count++
    result.add({ date, count, dayIndex: i })
```

### 7.2 预测单卡片未来复习路径

**函数**: `predictFutureReviewSchedule(record, numFutureReviews=5, now)`

模拟一张卡片从当前状态开始，N次全部成功复习后的时间线：

```
假设今天是2026-06-16，卡片当前状态：reps=2, interval=6, easeFactor=2.6

预测 5 次复习:
第1次: reps=3, interval=16, easeFactor=2.7 → 2026-07-02
第2次: reps=4, interval=43, easeFactor=2.8 → 2026-08-14
第3次: reps=5, interval=120, easeFactor=2.9 → 2026-12-12
第4次: reps=6, interval=348, easeFactor=3.0 → 2027-11-25
第5次: reps=7, interval=1044, easeFactor=3.0 → 2030-10-05
```

**注意**: easeFactor 的上限是 3.0，超过后不再增加。这个上限避免了 interval 无限增长导致卡片最终"永不出现"。

### 7.3 估算达成长期记忆需要多少天

**函数**: `estimateDaysToLongTerm(record, now)`

| 当前状态 | 估算天数 | 说明 |
|---------|---------|------|
| 新卡片（无记录） | ~67 天 | 1 + 6 + 16 + 44 = 67 |
| reps=1 | ~66 天 | 6 + 16 + 44 = 66 |
| reps=2 | ~60 天 | 16 + 44 = 60 |
| reps=3 | ~44 天 | 还需2次 |
| reps=4 | ~120 天 | 还需1次 |
| reps=5 | 0 天 | 已达成长期记忆 |

---

## 8. 记忆阶段划分

### 8.1 六个阶段定义

```
┌────────────────────────────────────────────────────────────┐
│ 记忆阶段判定流程图                                         │
│                                                            │
│  卡片有状态记录吗？ ── 否 → [未学习 unstarted]          │
│       │                                                    │
│       是                                                    │
│       ↓                                                    │
│  repetitions == 0? ── 是 → [失败回退 regressed]         │
│       │                                                    │
│       否                                                    │
│       ↓                                                    │
│  nextReviewAt <= now? ─ 是 → [到期复习 due]             │
│       │                                                    │
│       否                                                    │
│       ↓                                                    │
│  repetitions >= 5? ─ 是 → [长期记忆 long]               │
│       │                                                    │
│       否                                                    │
│       ↓                                                    │
│  repetitions >= 3? ─ 是 → [中期记忆 medium]            │
│       │                                                    │
│       否                                                    │
│       ↓                                                    │
│  其他情况 → [短期记忆 short]                              │
└────────────────────────────────────────────────────────────┘
```

### 8.2 记忆阶段的颜色和图标

| 阶段 | reps | nextReviewAt 条件 | 颜色 | 说明 |
|-----|------|------------------|------|------|
| 到期复习 due | 任意值 | <= now | 🔴 #FF6B6B | 需要立即复习 |
| 短期记忆 short | 1 ~ 2 | > now | 🟠 #FFA94D | 刚学习不久 |
| 中期记忆 medium | 3 ~ 4 | > now | 🟡 #FFD93D | 已掌握基础 |
| 长期记忆 long | >= 5 | > now | 🟢 #6BCB77 | 已形成长期记忆 |
| 未学习 unstarted | 无记录 | —— | ⚪ #C0C0C0 | 从未复习过 |
| 失败回退 regressed | 0 | —— | 🟥 #FF4757 | 上次失败，需重新开始 |

### 8.3 阶段分布计算

**函数**: `computeCategoryStageDistribution(cards, statuses, now)`

用于在分析视图中绘制环形图。

**函数**: `computeCategoryProgress(cards, statuses, now)`

计算"已掌握百分比"——中期记忆+长期记忆视为已掌握。

---

## 9. 测试评分与艾宾浩斯的联动

### 9.1 测试的定位

**测试 ≠ 复习**

- 复习 = 用户主动背诵，触发 SM-2 算法
- 测试 = 对当前掌握程度的抽样评估，不直接修改长期计划

### 9.2 测试评分对卡片状态的影响

当用户在单元检测/分类检测中答错一张卡片时：

```
测试答错卡片A → setCardStatus(cardId, categoryId, 'review', { mode: 'test' })

效果:
  status = 'review'                    ← 标记为待掌握
  reviewCount = reviewCount + 1        ← 计数+1
  repetitions = 保持不变               ← 不改变SM-2参数！
  interval = 保持不变
  easeFactor = 保持不变
  nextReviewAt = 保持不变

  addReviewHistory(..., mode='test')  ← 写入历史
```

### 9.3 测试后用户看到什么

```
测试中答错的卡片：
1. 在薄弱模式中出现（因为 status = 'review'）
2. 用户在薄弱模式中主动复习并标记"已掌握"
3. 此时才会真正调整 SM-2 参数：
   interval 缩短到 80%
   easeFactor 微增 0.05
   reps 保持不变
4. 下一次在艾宾浩斯模式中复习时，正常使用 applySM2WithDelay
```

### 9.4 测试历史的分析用途

reviewHistory 中的 mode='test' 记录可以用来：

- 统计某卡片在测试中被答错多少次（≥2 次时，UI显示警告信息）
- 识别用户的知识薄弱点
- 建议提前复习某些"在测试中反复出错"的卡片

```
在卡片详情弹窗中:
"⚠️ 此卡片在测试中答错 3 次，建议提前复习！"
```

---

## 10. 云端同步策略

### 10.1 分层同步原则

| 数据 | 本地存储 | 云端同步 | 原因 |
|------|---------|---------|------|
| cardStatus (mode='ebbinghaus') | ✅ | ✅ | 艾宾浩斯是长期计划，必须跨设备可继续 |
| cardStatus (其他 mode) | ✅ | ❌ | 临时浏览模式，状态不跨设备 |
| reviewHistory (全模式) | ✅ | ✅ | 连续学习天数分析需要完整历史 |
| studyPlans | ✅ | ✅ | 用户的设置需要跨设备一致 |
| categories/cards | ✅ | ✅ | 基础数据需要同步 |

### 10.2 换设备场景

```
设备A → 用户在艾宾浩斯模式下复习了50张
         同时在顺序模式下浏览了100张
         reviewHistory 记录了全部150次操作

云端同步 → cardStatus 中 50 条 ebbinghaus 记录上传
           reviewHistory 中 150 条记录上传
           studyPlans 上传

设备B → 登录同一账号
         ↓ 从云端拉取
         艾宾浩斯状态完整恢复（50张卡片的复习参数）
         复习历史完整（连续学习天数可用）
         顺序模式下的浏览记录不可见（设备B从头开始）
```

### 10.3 同步状态指示器

```
右上角显示:
✅ 已同步 06-16 14:30   ← 已同步完成，点击可手动触发同步
🔄 同步中...          ← 同步正在进行
🔄 从未同步            ← 新用户首次使用
```

### 10.4 仅本地数据警告条

当用户的 reviewHistory 中存在非 ebbinghaus 模式的数据时，在页面底部显示：

```
⚠️ 部分标记仅本设备记录
   顺序浏览、活跃学习、薄弱模式的标记不会同步到云端。
   艾宾浩斯计划已云端同步，换设备可无缝继续。
```

### 10.5 同步函数

```javascript
// 推送到云端（上传）
pushToCloud():
    for record in cardStatus:
        if record.mode == 'ebbinghaus':
            upload to cloud.card_status

    upload all reviewHistory records
    upload all studyPlans records

// 从云端拉取（下载）
pullFromCloud():
    fetch cloud.card_status → merge into local db.cardStatus
    fetch cloud.review_history → merge into local db.reviewHistory
    fetch cloud.study_plans → merge into local db.studyPlans

// 手动同步（用户点击同步按钮）
synchronize():
    await pushToCloud()
    await pullFromCloud()
    updateSyncStatus({ lastSyncAt: Date.now(), syncInProgress: false })
```

---

## 11. 关键代码文件索引

| 文件路径 | 主要功能 | 关键函数/组件 |
|---------|---------|-------------|
| `src/utils/ebbinghaus.js` | SM-2算法核心实现 | `applySM2WithDelay()`, `calculateDelayDays()`, `classifyDelayLevel()`, `predictFutureReviewSchedule()`, `estimateDaysToLongTerm()` |
| `src/utils/planCalculator.js` | 计划页计算工具（纯函数） | `getMemoryStage()`, `computeCategoryStageDistribution()`, `predictFutureDueCards()`, `computeCategoryProgress()`, `getStageName()`, `getStageColor()` |
| `src/pages/StudyPlan.jsx` | 计划页主组件 | `SyncIndicator` 组件, `MAIN_TABS` 配置, 数据加载逻辑 |
| `src/pages/PlanTodayView.jsx` | 今日计划视图 | 分类卡片列表, 模式汇总卡片, 进度条 |
| `src/pages/PlanFutureView.jsx` | 未来预测视图 | 柱状图实现, 超上限警告 |
| `src/pages/PlanAnalysisView.jsx` | 学习分析视图 | 环形图实现, 阶段分布图例, 分类进度表 |
| `src/pages/StudyPlan.css` | 计划页样式 | 进度条, 环形图, Tab样式, 响应式布局 |
| `src/pages/Memorize.jsx` | 背诵页主组件 | `handleMark()`, `setCardStatus` 调用, fromPlan 参数处理 |
| `src/services/db.js` | IndexedDB 数据库层 | `setCardStatus()` (核心：模式隔离逻辑), `addReviewHistory()`, `getAllCardStatuses()`, `upsertStudyPlan()`, v6 版本升级 |
| `src/services/sync.js` | 云端同步层 | `pushToCloud()`, `pullFromCloud()`, `synchronize()`, `getSyncStatus()`, 分层上传逻辑 |
| `src/services/testGradingService.js` | 测试评分服务 | `updateMasteryAndWrongAnswers()` 中传入 `mode: 'test'` |
| `src/pages/StatsLongTerm.jsx` | 长期记忆卡片页 | 显示reps>=5的卡片详情 |
| `src/App.jsx` | 路由配置 | `/memorize/plan` 路由, `/stats/longterm` 路由 |
| 数据库版本 | v6 升级 | 新增 reviewHistory、studyPlans 表, cardStatus.mode 字段 |

---

## 附录 A：参数常量

| 常量 | 值 | 含义 | 定义位置 |
|-----|-----|------|---------|
| DAY_MS | 86400000 | 一天的毫秒数 | ebbinghaus.js |
| initial easeFactor | 2.5 | 初始难度系数 | ebbinghaus.js applySM2WithDelay |
| min easeFactor | 1.3 | 难度系数下限 | ebbinghaus.js |
| max easeFactor | 3.0 | 难度系数上限（预测时使用） | planCalculator.js |
| dailyReviewLimit (default) | 50 | 默认每日复习上限 | studyPlans DEFAULT_STUDY_PLAN |
| dailyNewLimit (default) | 20 | 默认每日新学上限 | studyPlans DEFAULT_STUDY_PLAN |
| longTerm threshold | 5 | reps>=5视为长期记忆 | planCalculator.js getMemoryStage |
| test warning threshold | 2 | 测试答错≥2次显示警告 | UI逻辑 |

---

## 附录 B：卡片生命周期示例

一张卡片从首次学习到长期记忆的完整旅程：

```
Day 0 (6月16日): 首次学习，标记"已掌握"
      → reps=1, interval=1, easeFactor=2.6, nextReview=6月17日

Day 1 (6月17日): 按时复习，标记"已掌握"
      → reps=2, interval=6, easeFactor=2.7, nextReview=6月23日

Day 7 (6月23日): 按时复习，标记"已掌握"
      → reps=3, interval=ceil(6×2.7)=17, easeFactor=2.8, nextReview=7月10日

Day 24 (7月10日): 按时复习，标记"已掌握"
      → reps=4, interval=ceil(17×2.8)=48, easeFactor=2.9, nextReview=8月27日

Day 72 (8月27日): 按时复习，标记"已掌握"
      → reps=5, interval=ceil(48×2.9)=140, easeFactor=3.0, nextReview=1月14日
      → ✓ 进入长期记忆！

如果期间出现延迟或失败，时间线会被相应延长或重置。
```

**报告结束**
