# 冗余清理执行记录（第 1-4 批）

> 执行时间：2026-09-20
> 基线提交：`b92ae18`（清理前完整快照）
> 清理提交：`af4f965`
> 仓库：https://github.com/egg-Li-dd/eggli-flashcard-app （私有）

## 一句话结论

移出 **1286 MB 构建产物** + **5505 行零耦合死代码** + **189 个 IDE 规划文档** + **34 个临时脚本**（含 15 个带明文密钥的）。
项目体积（不含 `node_modules`）**1496 MB → 210 MB**，仓库文件数 **525 → 320**。
**构建通过，测试零回归。**

## 安全网：所有移除都是「移走」，没有硬删

本环境无法调用回收站（Bash 调 PowerShell 被安全策略拦截，`Add-Type` 也被拦），
因此改用**项目外的命名归档目录**，完整保留原目录结构：

| 归档位置 | 内容 |
|---|---|
| `../_cleanup_archive_2026-09-20/__root_scripts__/` | 34 个根目录临时/调试脚本 + 2 个 payload 转储 |
| `../_cleanup_archive_2026-09-20/__dead_code__/` | 死代码（保留 `src/` 原路径结构） |
| `../_cleanup_archive_2026-09-20/.trae/` | 189 个 Trae IDE 规划文档 |
| `../_cleanup_archive_2026-09-20/android/...` | 构建产物与缓存 |
| `../_release_archive/v2.1/` | 92 MB 的 v2.1 发布 APK |

需要回退任何一项：直接从归档目录 `mv` 回原位即可。

## 第 1 批：构建产物与 IDE 缓存（1286 MB，零代码风险）

| 路径 | 体积 |
|---|---|
| `android/app/build` | 1200 MB |
| `android/app/src/main/assets` | 30 MB（Capacitor 同步产物，`npx cap sync android` 可重建） |
| `dist` | 30 MB（Vite 构建输出，`npm run build` 可重建） |
| `android/.gradle` | 21 MB |
| `android/capacitor-cordova-android-plugins` | 5 MB |
| `android/build`、`android/.kotlin`、`plugins/build`、`.idea` | 各 1 MB |
| 根目录日志与截图（`localhost-*.log`、`screen_test*.png`） | 1.5 MB |

## 第 2 批：临时脚本与 IDE 残留

- **34 个根目录临时/调试脚本**，其中 **15 个明文写死 CloudBase `service_role` 密钥**（最高权限、可绕过 RLS）。
  这些从未进过 git（`.gitignore` 已排除），但**建议轮换成环境变量读取 + 轮换密钥**。
- **`.trae/` 189 个文件** —— Trae IDE 的 AI 规划文档，占仓库文件数第一，比 `src/` 还多。

## 第 3 批：文档与 SQL 归位

| 动作 | 数量 |
|---|---|
| 根目录中文报告 → `docs/reports/` | 29 份 |
| 当前生效 SQL → `db/`（`cloudbase-postgresql-schema.sql`、`cloudbase_rls_policy.sql`、`migrate_knowledge_tree.sql`、`删除所有表.sql`） | 4 份 |
| 历史 SQL（2026-06-20 及更早，含 `supabase_init.sql`）→ `db/archive/` | 15 份 |

根目录现在只剩 **12 个文件**：`README.md` + 3 个构建配置 + 3 个页面/配置文件 + 3 个配置目录入口。

## 第 4 批：死代码（5505 行）

| 文件 | 行数 | 判定依据 |
|---|---|---|
| `src/services/ai/`（10 文件） | 3344 | 全项目**只有它自己的测试**引用；真实 AI 逻辑全在 `aiService.js`（13731 行）。这是一套从未接入的平行架构 |
| `src/__tests__/classificationService.test.js` | 238 | 上述架构的专属测试，且本身就在失败 |
| `src/components/TopicGroupConfirm.jsx` | 550 | 零 import |
| `src/services/aiCardGenerationFlow.js` | 420 | 零 import |
| `src/pages/SettingsCloud.jsx` | 393 | 零 import，且设置菜单里没有入口 |
| `src/components/DuplicateCardConfirm.jsx` | 362 | 零 import（`Category.jsx` 里的两处引用是"已清理"的历史注释） |
| `src/services/redis.js` | 198 | 8 个导出全零调用；`Category.jsx` 里那行 import 的两个符号**全项目各只出现 1 次 = import 行本身**，已一并删除 |

`src/` 文件数 **162 → 146**。

## 验证结果

### 构建
```
npm run build  →  ✓ built in 1.94s   （0 错误）
```

### 测试零回归（用 git stash 做基线对照）

| | 测试文件 | 失败文件 | AssertionError | ReferenceError | 模块解析错误 |
|---|---|---|---|---|---|
| **基线** `b92ae18` | 20 | 9 | 35 | 1 | **0** |
| **清理后** `af4f965` | 19 | 8 | **35** | **1** | **0** |

**错误类型计数完全一致** → 本次清理**零回归**。
测试文件从 20 → 19 是因为删掉的 `classificationService.test.js` 本身就在失败之列（9 failed → 8 failed，passed 恒为 11）。

### 密钥扫描
全项目仅剩 `.env` 中的 **`anon` 公开键**（CloudBase 设计上就要打进客户端包，安全）。
`service_role` 密钥已全部移出项目。

## 已知遗留（与本次清理无关）

- **39 个测试失败是原有问题**，集中在 `testQuestionValidation.test.js` 等（`validateQuestion` 弱模型畸形题目防御）。基线即存在，清理后数量不变。待单独处理。
- `Category.jsx` 中有两处提到 `DuplicateCardConfirm` 的历史注释，功能无影响，未动。

## 下一批（第 5-7 批，尚未执行）

| 批次 | 内容 | 牵连范围 |
|---|---|---|
| 5 | 自建 PaddleOCR（`paddleocr_server/`，端口 8000） | `paddleOcr.js` + 设置页一个配置区块 + `aiService.js` 一处 import。**耦合浅** |
| 6 | Tailscale | 前端 12 文件 + 安卓 10 文件；重点是 `android/app/libs/libtailscale.aar` **66 MB**（APK 92 MB 的主因），删除后 APK 预计降到 30 MB 以内 |
| 7 | PC 引擎（端口 19000） | 15 文件、700+ 处引用；`aiService.js` 有 7 处 `callPcEngineAi()`，`pcEngineFallback.js`(25 处)、`AppContext.jsx`(25 处)。**耦合最深、风险最高** |

> 注意：`paddleOcrLocal.js`（前端 WebView 内 PaddleJS 离线推理）**不依赖任何后端，不要删**。
> `voskService.js` 与系统悬浮窗三件套同理，都是独立的安卓设备功能。
