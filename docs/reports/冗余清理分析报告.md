# eggLi 应用冗余清理分析报告

> 生成时间：2026-09-19
> 分析范围：`C:\creategame\EggLi-Flashcards\eggli-flashcard-app`
> 方式：静态引用分析（谁 import 谁）+ 目录体积统计，未修改任何文件

---

## 一、总览

| 分类 | 可清理内容 | 代码量 | 磁盘 |
|---|---|---|---|
| A. Tailscale（确认删除） | 前端 2 文件 + 安卓 5 文件 + 1 个 AAR | ~3,100 行 | **66 MB** |
| B. 本地后端（确认删除） | PC 引擎 4 文件 + 自建 PaddleOCR 服务 | ~2,500 行 | - |
| C. 死代码（未接入） | `ai/` 整套架构 + 4 个孤立文件 | ~5,270 行 | - |
| D. 开发过程产物 | `.trae/` + `.idea/` + 根目录临时脚本 | - | **~95 MB** |
| E. 构建产物（可重建） | `android/app/build` / `dist` / `.gradle` | - | **1,251 MB** |
| F. 历史 SQL / 脚本 | 11 份过期 SQL + 19 份运维脚本 | - | - |
| **合计** | | **~11,000 行** | **~1.4 GB** |

预计清理后：`src/` 从 11.1 万行降至约 **10 万行**，APK 从 92 MB 降至约 **30 MB 以内**。

---

## 二、A 类：Tailscale 相关（确认删除）

### A1. 可直接整文件删除

| 文件 | 行数 | 说明 |
|---|---|---|
| `src/services/tailscale.js` | 290 | 封装 libtailscale LocalAPI，16 个导出 |
| `src/pages/SettingsTailscale.jsx` | 885 | Tailscale 设置页 |
| `android/.../plugins/TailscaleVPNPlugin.kt` | 370 | Capacitor 插件桥 |
| `android/.../plugins/AndroidAppContext.kt` | 245 | 实现 `libtailscale.AppContext` 接口 |
| `android/.../services/TailscaleRemoteService.kt` | 574 | IPN 引擎常驻服务 |
| `android/.../services/TailscaleVpnService.kt` | 178 | 继承 `VpnService` |
| `android/.../services/VPNServiceBuilderImpl.java` | 80 | VPN 建立实现 |
| `android/app/libs/libtailscale.aar` | — | **66 MB，APK 体积的主要来源** |
| `android/app/libs/libtailscale-sources.jar` | — | 21 KB |

### A2. 需要改造的集成点（不能只删文件）

| 文件 | 位置 | 处理方式 |
|---|---|---|
| `src/App.jsx` | L17 路由 import、L145 状态解构、L302-331 启动自动连接、L412-478 心跳检测、L516 路由 | 删除该路由 + 整个自动连接 effect + 心跳中的 Tailscale 分支 |
| `src/context/AppContext.jsx` | L228-229 初始状态、L245-246 运行时状态、L358-359、L481、L605-606、L650、L1104 | 删除 `tailscaleAuthKey` / `tailscaleConnected` 相关 state 与 action |
| `src/components/ConnectionStatusBar.jsx` | 整文件 78 行 | **纯 TS+PC 状态指示器，直接删除**，并从 App.jsx 移除引用 |
| `src/services/settingsSync.js` | L61-63 | 从同步字段白名单移除 `tailscaleAuthKey` / `tailscaleAutoConnect` |
| `src/pages/CloudData.jsx` | L105、L357、L364-365、L511、L518-519 | 删除设置导入导出中的 tailscale 字段 |
| `src/pages/CloudDataDetail.jsx` | L450、L614、L621-622 | 同上 |
| `android/.../MainActivity.java` | L6 import、L17 `registerPlugin` | 删除 |
| `android/app/src/main/AndroidManifest.xml` | L39-58 两个 service 声明、L80 `FOREGROUND_SERVICE_VPN` 权限 | 删除 |
| `android/app/build.gradle` | L89 `implementation files('libs/libtailscale.aar')` | 删除 |
| `android/app/src/main/assets/capacitor.plugins.json` | — | 移除 TailscaleVPN 条目 |
| `android/app/src/main/res/values/strings.xml` | — | 移除 tailscale 相关字符串 |
| `android/.../res/xml/network_security_config.xml` | 末尾 localhost / 127.0.0.1 域 | 移除（注释已写明是为 Tailscale LocalAPI） |
| 上级目录 `tailscale-android-main/` + `.zip` | — | 第三方 Tailscale 源码，本地拉取用于编译 AAR |

---

## 三、B 类：本地后端（确认删除）

「本地建后端」在本项目里是**两个独立服务**，需分别处理。

### B1. PC 引擎（端口 19000，跑在用户自己电脑上）

| 文件 | 行数 |
|---|---|
| `src/services/pcEngine.js` | 561 |
| `src/services/pcEngineProxy.js` | 583 |
| `src/services/pcEngineFallback.js` | 235 |
| `src/pages/SettingsPcEngine.jsx` | 412 |

**这是耦合最深的一块**，必须逐个改造：

| 文件 | 位置 | 处理方式 |
|---|---|---|
| `src/services/aiService.js` | L44-45 import、L68-84 `callPcEngineAi()` 函数、**L107 / L196 / L534 / L893 / L2965 / L5542 共 7 处调用点** | 删除函数与全部调用点，保留原有 AI 服务分支 |
| `src/components/InputBar.jsx` | L12-13 import、L677 语音、L1025-1026 配置、L1037 文档解析 | 移除 `pc-engine-voice` 分支与 `parseDocument` 调用 |
| `src/pages/Category.jsx` | L14-15 import、L2335-2339 | 将 `ocrWithFallback` 替换为普通 OCR 调用链 |
| `src/pages/SettingsAiService.jsx` | L532-541 | 删除「PC 引擎已配置」提示区块 |
| `src/pages/SettingsSpeech.jsx` | L919-920 | 删除 PC 引擎语音状态显示 |
| `src/pages/CloudData.jsx` / `CloudDataDetail.jsx` | pcEngineConfig 字段 | 删除导入导出字段 |
| `src/context/AppContext.jsx` | 25 处 state / action / storage key | 删除 `pcEngineServer/Port/Token/ParseEngine/Asr/Connected` |
| `src/App.jsx` | 12 处 | 路由、心跳、`setPcEngineConnected` |
| `src/utils/constants.js` | L19 `AI_SERVICE_MODES` 的 `pc-engine` 项、L239-243 `SPEECH_MODES` 的 `pc-engine-voice` 项、L139-143 `STORAGE_KEYS.PC_ENGINE_*` | 删除 |

### B2. 自建 PaddleOCR HTTP 服务（端口 8000）

| 文件 | 行数 | 说明 |
|---|---|---|
| `paddleocr_server/`（3 文件） | — | `server.py` / `requirements.txt` / `README.md` |
| `src/services/paddleOcr.js` | 122 | 调用自建服务 |
| `SettingsOcr.jsx` L667-693 | — | 「PaddleOCR 自建服务」配置区块 |
| `utils/constants.js` L116 `PADDLEOCR_SERVER_URL`、L157-161 `OCR_ENGINES` 的 `paddleocr-server` 项 | — | 删除 |
| `aiService.js` L39 `extractTextWithPaddleOcr` | — | 删除 import 与调用分支 |

> ⚠️ **不要误删** `src/services/paddleOcrLocal.js`（490 行）—— 那是**前端 WebView 内离线推理**（PaddleJS），不依赖任何后端服务，与自建服务是两回事。

---

## 四、C 类：死代码（未接入，可安全删除）

### C1. `src/services/ai/` 整套架构 —— 最大一块死代码

**10 个文件、3,344 行，全项目只有它自己的单元测试引用它。**

```
src/services/ai/AiServiceInterface.js          49
src/services/ai/ClassificationService.js     1027
src/services/ai/ClassificationStateMachine.js 156
src/services/ai/dataModels.js                 460
src/services/ai/jsonParser.js                 171
src/services/ai/adapters/DashscopeAdapter.js  398
src/services/ai/adapters/DeepSeekAdapter.js   417
src/services/ai/adapters/SparkLiteAdapter.js  405
src/services/ai/adapters/VolcanoEngineAdapter.js 257
src/services/ai/adapters/index.js               4
```

真实 AI 逻辑全在 `src/services/aiService.js`（13,731 行）。这是一套**平行架构**，从未被生产代码接入。
若删除，需一并删除 `src/__tests__/classificationService.test.js`。

### C2. 其他孤立文件

| 文件 | 行数 | 证据 |
|---|---|---|
| `src/components/TopicGroupConfirm.jsx` | 550 | 零 import |
| `src/services/aiCardGenerationFlow.js` | 420 | 零 import |
| `src/pages/SettingsCloud.jsx` | 393 | 零 import，且不在 `SettingsMain` 菜单里 |
| `src/components/DuplicateCardConfirm.jsx` | 362 | 零 import |
| `src/services/redis.js` | 198 | 8 个导出全部零调用；`Category.jsx` L16 仅有一个**未使用的 import** |

### C3. 备份与杂项

- `src/components/InputBar.jsx.bak`
- `src/pages/Category.jsx.bak`
- `src/__tests__/_debug.mjs`（测试目录里的调试脚本）
- 空文件：`write_account.js`、`报错文档.txt`、`更新日志.txt`

---

## 五、D 类：开发过程产物

| 项目 | 体积 | 说明 |
|---|---|---|
| `.trae/` | 2 MB / **189 文件** | Trae IDE 的 AI 规划文档（specs 50+ 个专题 × checklist/spec/tasks） |
| `.idea/` | 1 MB | JetBrains IDE 配置 |
| `_*.mjs`（6 个） | — | `_checktags` / `_debug` / `_find_err` / `_gen_standalone` / `_serve` / `_test_js`，临时调试脚本 |
| 根目录运维脚本 19 个 | — | `check_rls.mjs`、`exec_sql.mjs`、`fix_rls.mjs`、`run_pgsql.mjs`、`test_*.mjs` 等一次性脚本 |
| 日志 / 截图 | — | `localhost-1781886429120.log`、`screen_test.png`、`screen_test2.png`、`_db_temp.txt`、`_line1.txt` |
| `.github/workflows/ci.yml` | 1 KB | 需确认是否真在用 GitHub Actions，不用则删 |

**根目录必须保留的 4 个配置文件**：`vite.config.js`、`vitest.config.js`、`eslint.config.js`、`.env.example`

---

## 六、E 类：构建产物（可随时重建）

| 目录 | 体积 |
|---|---|
| `android/app/build` | **1,200 MB** |
| `dist/` | 30 MB |
| `android/.gradle` | 21 MB |
| `android/build` | 1 MB |

`node_modules`（305 MB）建议保留，重装成本高于收益。
`egg李-flashcards-v2.1-release.apk`（92 MB）按需保留 —— 若要归档分发版则留下。

---

## 七、F 类：SQL 与脚本版本

### 当前有效（2026-07-10 之后）

- `cloudbase-postgresql-schema.sql`（22 KB）
- `cloudbase_rls_policy.sql`（13 KB）
- `migrate_knowledge_tree.sql`（7 KB）
- `删除所有表.sql`（1 KB，工具脚本）

### 历史迭代（2026-06-20 之前，共 11 份可删）

`sql_cloudbase_part1/2`、`sql_cloudbase_fix`、`sql_cloudbase_fix_part1/2`、`sql_cloudbase_final`、`sql_check_rls`、`sql_full_no_rls`、`sql_part1_tables`、`sql_part2_remaining_tables`、`sql_part3_indexes`、`sql_part4_rls_part1`、`sql_part5_rls_part2`、`sql_verify_tables`

### 已废弃技术栈

- `supabase_init.sql`（2026-06-13）—— 已迁移至腾讯云 CloudBase，`@supabase/supabase-js` 也已不在依赖中
  > ⚠️ `cloudbase.js` 中仍有 `isValidSupabaseUrl`、`resetSupabaseClient` 等**函数名残留**，那是兼容性命名，被页面正常调用，**不要删**（改名为纯美化操作，风险大于收益）

---

## 八、看似冗余但必须保留

| 项目 | 为什么保留 |
|---|---|
| `src/services/paddleOcrLocal.js` | 前端 WebView 内离线 OCR，不依赖后端 |
| `src/services/voskService.js` | APP 内离线语音识别，不是本地后端 |
| `src/services/systemFloatingWindow.js` + `SystemFloatingWindowPlugin.kt` + `FloatingWindowPage.jsx` | 安卓系统悬浮窗，独立功能，不依赖 PC 引擎/Tailscale |
| `src/services/baiduOcr.js` / `tesseractOcr.js` / `visionAi.js` | 均为 SettingsOcr 中仍在用的引擎选项 |
| `src/pages/PlanTodayView` / `PlanFutureView` / `PlanAnalysisView` / `PlanSettingsModal` | 由 `StudyPlan.jsx` 间接引用，是活的 |
| 根目录中文报告 `.txt` / `.md` | 你的项目文档资产，与代码无关，建议单独归档 |
| `src/services/dashscope.js` / `deepseek.js` / `iflytekAi.js` / `volcanoEngine.js` | 四家 AI 服务商适配层，均在用 |

---

## 九、建议执行批次

按风险从低到高，每批独立可验证：

| 批次 | 内容 | 风险 | 验证方式 |
|---|---|---|---|
| **第 1 批** | 构造产物清理：`android/app/build`、`android/.gradle`、`android/build`、`dist` | 🟢 极低 | `npm run build` 能重建 |
| **第 2 批** | D 类开发产物：`.trae/`、`.idea/`、`_*.mjs`、根目录运维脚本、日志截图、空文件 | 🟢 极低 | 不影响 dev / build |
| **第 3 批** | C 类死代码：`src/services/ai/` 整目录 + 4 个孤立文件 + 2 个 `.bak` + 对应测试 | 🟢 低 | `npm test` 通过 |
| **第 4 批** | F 类历史 SQL 与过期脚本 | 🟢 低 | 无代码依赖 |
| **第 5 批** | B2 自建 PaddleOCR 服务（含前端分支） | 🟡 中 | OCR 走 `paddleocr-local` / 百度云 / AI 视觉 |
| **第 6 批** | A 类 Tailscale（前端 + 安卓 + AAR） | 🟡 中 | 需重新构建 APK 验证 |
| **第 7 批** | B1 PC 引擎（含 `aiService.js` 7 处调用点） | 🔴 高 | 需跑一遍 AI 出题 / 分类 / 语音全链路 |

**安全约定**：每批执行前先建快照，删除走回收站（不硬删），单批完成后跑一次 `npm run build` + `npm test` 确认无回归。

---

## 十、需要你确认的 3 个问题

1. **`egg李-flashcards-v2.1-release.apk`（92 MB）** 要保留归档，还是一并清掉？
2. **根目录的中文报告文档**（`文档分布.txt`、`背诵功能实现.txt`、`易报错事项.txt` 等约 30 份、共 600 KB）—— 是移到 `docs/` 统一归档，还是原位保留？
3. **`.github/workflows/ci.yml`** 你还在用 GitHub Actions 吗？不用的话可归入第 2 批。
