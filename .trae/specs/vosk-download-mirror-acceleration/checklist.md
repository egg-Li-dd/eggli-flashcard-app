- [x] Checkpoint 1: `voskService.js` 中新增了 `VOSK_MIRROR_KKGITHUB`、`VOSK_MIRROR_BGITHUB`、`VOSK_MIRROR_GHPROXY` 三个镜像源常量
- [x] Checkpoint 2: `VOSK_MODELS` 中每个模型的 `mirrorUrls` 数组包含 kkgithub、bgithub、ghproxy 三个代理 URL，URL 格式正确
- [x] Checkpoint 3: `getOrderedSources(modelId, userSelectedKey)` 辅助函数正确实现，按"用户选中 → 蓝奏云 → kkgithub → bgithub → ghproxy → GitHub 直连 → 主源"顺序返回，过滤 null
- [x] Checkpoint 4: `SettingsSpeech.jsx` 中 `downloadSource` 默认值在小模型时为 `'lanzou'`，大模型时为 `'mirror_kkgithub'`
- [x] Checkpoint 5: 切换模型时默认下载源自动调整（小模型→蓝奏云，大模型→kkgithub）
- [x] Checkpoint 6: 下载源选择器 UI 包含所有镜像源选项，每项有清晰的中文说明（如 "kkgithub 镜像"、"蓝奏云（国内最快）"）
- [x] Checkpoint 7: `handleDownloadModel` 使用 `getOrderedSources` 构建源列表，正确传递给 `downloadVoskModelWithFallback`
- [x] Checkpoint 8: 当前下载源显示文案能区分 "GitHub 代理（kkgithub/bgithub/ghproxy）" 与原 "GitHub 直连"
- [x] Checkpoint 9: Android 原生端能正确处理代理镜像 URL（kkgithub/bgithub 标准 HTTPS 直链；ghproxy 嵌套 https:// URL 由 OkHttp/HttpURLConnection 原样发送，路径中的 // 不会被归一化）
- [x] Checkpoint 10: 前端 `npm run build` 通过（exit code 0，2m33s）
- [x] Checkpoint 11: Android `assembleDebug` 编译通过（BUILD SUCCESSFUL in 19s）
- [x] Checkpoint 12: Debug APK 打包成功，输出路径：android/app/build/outputs/apk/debug/app-debug.apk
