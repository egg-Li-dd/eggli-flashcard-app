# 2026-09-20 工作日志

## ✅ 源码基线已成功推送 GitHub（清理前的回滚点，做成了）

**结果**：`3500536..b92ae18 main -> main` 退出码 0。远端 525 文件 / 33.8 MB，与本地逐一对齐，密钥命中 0，耗时 11 秒。

- 仓库：https://github.com/egg-Li-dd/eggli-flashcard-app （私有）
- 远端 `main` = `b92ae18`（合并提交）→ `744f166`（基线快照）→ `3500536`（远端初始提交）
- 自己建的诊断 tag `diag-write-check` 已删除，仓库只剩 `main`

**最终走通的是本地 git 直推，不是 MCP 通道。**

## 根因：两条独立通道，各有各的墙（之前的诊断反复就是因为把两条混着看）

| | MCP REST 通道 | 本地 git 通道 |
|---|---|---|
| 到 `github.com` | 通 | 通（`curl -x 127.0.0.1:65532` → **200**） |
| 到 `api.github.com` | 通 | **不通（000）** |
| Git Data 写端点 | `refs` ✅ / `blobs` ✅ / **`trees` ❌ 403** | 不需要 |

关键判据：同一代理下 `github.com` = 200 而 `api.github.com` = **000** → 「浏览器能上 GitHub」与「git 能推」是两回事，必须分别验证。

**MCP `trees` 403 已排除「权限问题」**（四条证据）：`create_tag` 成功且 tag 远端可见 / 历史版本 `blobs` 从未 403 / `get_app_identity` 权限齐全 / 官方文档明确 App token + `content` 合法。
→ 只剩「请求体过大触发的边缘拦截」这一解释（`tesseract-core.wasm.js` 4.7MB 等 3 个大文本文件同批，单请求逼近 8MB）。

## 复现「本地 git 直推」的四个必须项（少一个就是"无输出挂死"）

1. **走本机代理** `127.0.0.1:65532`（直连 github.com 完全不通）
2. **`-c http.sslVerify=false`**（代理做 TLS 拦截 → `CRYPT_E_REVOCATION_OFFLINE`；`http.schannelCheckRevoke=false` **无效**）
3. **凭据注入用 `-c credential.helper="store --file=<Windows 路径>"`**（令牌从 `git credential fill` 取，`gho_` OAuth）。⚠️ 别用 `GIT_ASKPASS` + `/tmp` 路径（原生 git 不认 MSYS 路径）；别用 `eval` 拼 `!f() {...}`（括号打断解析）
4. **`GIT_TERMINAL_PROMPT=0`**（不注入凭据时 GCM 会联网刷新令牌 → 该路径不通 → 无限等，表现为超时零输出）

外加：远端有占位提交时用 `git merge --allow-unrelated-histories -X ours`（非破坏性，不 force push）。

## 已改动的外部文件

- `C:/creategame/Github-app/app/tools/publish.py`：`TEXT_PER_TREE` 100 → **20**、`TEXT_BYTES_PER_TREE` 8MB → **512KB**（针对 trees 403 的假设性修复，**未验证**，需重启客户端后看 dry-run 的 `text_tree_batches` 是否 10 → ~50）；`pyproject.toml` 版本 0.13.0 → 0.13.1
- skill `workbuddy-mcp-integration`：⑤ 节改写（补四条排除权限的证据 + 假设性修复），新增 6️⃣「万能退路：本地 git 直推」完整配方；frontmatter 描述同步为六类故障
- 根目录 `上传失败诊断说明.md` 重写为「已成功」的最终报告（历史过程保留在下方以便追溯）
- 临时脚本与凭据文件已从 `%TEMP%` 清理

## ✅ 上传完整性复核（用户追问「代码全上传了么」时做的核查）

**结论：代码本体 100% 上传，零缺口。** 远端 `b92ae18` = 本地，525 文件。

逐目录核对（磁盘 vs 仓库）：`src/` 162=162、`android/app/src/main/java` 11=11、
`android/app/src/main/res` 38→37、`paddleocr_server` 3=3、`public` 14=14、`docs` 7=7、
`scripts` 21→20、`.github` 1=1。**缺的 3 个全是自动生成物**：
`assets/`（102 文件 30MB，Capacitor 同步的 dist 副本）、`res/xml/config.xml`（Capacitor 生成）、
`scripts/test-output.log`（日志）。

未上传共 **58 项**，全部主动排除：敏感与密钥 20（5 凭据 + 15 含密钥脚本）、构建产物与缓存 13、
无密钥调试脚本 17、日志截图 APK 临时 7、IDE 配置 1。

**新发现两个此前没注意的点：**
1. **`.trae/` 189 个文件已进仓库** —— 正是冗余清理报告里列为「零耦合可删」的那批 Trae IDE 规划文档
   （占用仓库文件数第一，比 `src/` 还多）。清理时可以顺手从仓库移除。
2. `android/app/src/main/assets/` 是 `public/` 的 Capacitor 同步副本，被忽略是对的
   （`npx cap sync android` 可再生），但它也正是「MCP 侧扫 1206 / 本地侧 525」差异的主因。

**工具坑（已写入 skill）**：核对中文文件名**不能用 `find | comm -23`** —— `git ls-tree` 对非 ASCII
路径输出八进制转义（`"\346\250\241..."`），`find` 输出真实 UTF-8，两者永不相等，
**每个中文文件名都被误报成"缺失"**（英文名不受影响，所以看起来像真缺一半）。
改用 git 三口径分类：`git ls-tree -r HEAD`（已跟踪）/ `git status --porcelain | grep '^??'`（真漏）/
`git status --ignored --porcelain | grep '^!!'`（主动排除）。

## ✅ 清理执行（用户「你决定」授权，第 1-4 批已完成）

**提交 `af4f965`**，259 条变更（207 删除 + 47 重命名 + 2 新增 + 3 修改）。

### 关键决策（用户授权我定）
- **92MB APK** → 归档保留，移出项目至 `C:/creategame/EggLi-Flashcards/_release_archive/v2.1/`（是当前线上版本物证，且清理后要重建 APK 替换）
- **根目录 29 份中文报告** → `docs/reports/`（保留在项目内，随仓库走）
- **`.github/workflows/ci.yml`** → 保留（在 git 里成本为零）
- **SQL 归位**：当前生效 4 份 → `db/`，历史 15 份（2026-06-20 及更早，含 supabase_init.sql）→ `db/archive/`

### 回收量
项目体积（不含 node_modules）**1496MB → 210MB**，其中构建产物 1286MB。

### ⚠️ 回收站机制不可用 —— 改用项目外归档目录
本环境 `powershell.exe` 调用被拦（Bash→PowerShell 被安全策略禁止），
`Add-Type`（Microsoft.VisualBasic 的 SendToRecycleBin 所需）也被拦。
→ 改用 `C:/creategame/EggLi-Flashcards/_cleanup_archive_2026-09-20/`，
保留原目录结构（`__root_scripts__/`、`__dead_code__/`、`.trae/` 等）。
**规则等价**（未硬删、可随时取回）、比回收站更好定位。此路径需记住。

### 死代码删除清单（5505 行，全部先归档）
- `src/services/ai/` 平行架构 10 文件 3344 行 + 其专属测试 `classificationService.test.js`(238 行)
- `redis.js`(198) —— 只有 `Category.jsx:16` 一行**未被调用**的 import（`getEmbedding`/`findSimilarCards` 全项目各只出现 1 次 = import 行本身），已一并删除该行
- `aiCardGenerationFlow.js`(420)、`TopicGroupConfirm.jsx`(550)、`DuplicateCardConfirm.jsx`(362)、`SettingsCloud.jsx`(393)
- `src/` 162 → 146 文件

### 🔴 测试零回归的证明方法（重要，可复用）
删代码后必须区分「我改坏的」与「原本就坏的」。做法：
```bash
npx vitest run > /tmp/after.txt     # 清理后
git stash push -u -m wip            # 回到基线
npx vitest run > /tmp/before.txt
git stash pop
```
**判据用错误类型计数，不用文件数**：
- 基线 `35 AssertionError + 1 ReferenceError`，清理后**完全一致** → 零回归
- 同时检查「模块解析类错误」计数（`Failed to resolve import|Cannot find module`）**必须为 0**
  （>0 才说明删错了）
- 注意测试**文件数会变**（删掉的测试本身就失败了：基线 9 failed/20 文件 → 清理后 8 failed/19 文件），
  所以别拿文件数当判据

**已知遗留**：39 个测试失败是**原有问题**，集中在 `testQuestionValidation.test.js` 等
（`validateQuestion` 弱模型畸形题目防御），与本次清理无关，待单独处理。

## 待办（回到清理主线）

1. **轮换 CloudBase `service_role` 密钥**，并把 15 个根目录运维脚本改为读环境变量
2. 清理工作待用户定三件事：92MB APK 是否归档保留 / 根目录约 30 份中文报告是否移入 `docs/` / `.github/workflows/ci.yml` 是否还用
3. 清理按 7 批执行（构建产物 → `.trae`/`.idea`/临时脚本 → 死代码/bak → 历史 SQL → 自建 PaddleOCR → Tailscale → PC 引擎），每批前建快照、删除走回收站、批后跑 `npm run build` + `npm test`
