# ======================================
# AI 背诵卡片 — 环境变量配置
# ======================================
# 复制此文件为 .env 并填入真实值
#   cp .env.example .env
# ======================================

# CloudBase 配置
# 腾讯云 CloudBase 环境 ID（已在 cloudbase.js 中硬编码，此处可覆盖）
# VITE_CLOUDBASE_ENV=your-cloudbase-env-id

# CloudBase Publishable Key（客户端安全密钥，用于 v3 认证 API）
# 前往 云开发平台 → API Key 配置 生成 Publishable Key（角色为 anon，非 service_role）
# VITE_CLOUDBASE_ACCESS_KEY=your-publishable-key

# 管理员密码预设（可选，SHA-256 哈希值）
# 如果设置了此项，"设置→云端同步→高级配置"中的管理密码将被预置
# 任何人（含开发者）只能验证此密码，不能重新设置
# 生成方式：浏览器控制台执行 → await crypto.subtle.digest('SHA-256', new TextEncoder().encode('你的密码')).then(h => Array.from(new Uint8Array(h)).map(b => b.toString(16).padStart(2, '0')).join(''))
# VITE_ADMIN_PASSWORD_HASH=
