# Checklist

- [x] `generateNewTopicsAndUnits` 的 assignments 输出中不含 `unitId` 和 `chapterId`
- [x] 强模型路径：`generateNewTopicsAndUnits` 单次调用，输出仅含 `newChapterName` / `newUnitName`
- [x] 弱模型路径：`generateNewTopicsAndUnits` 多轮分批，Round1+Round2，输出仅含 `newChapterName` / `newUnitName`
- [x] 弱模型 Round2 超时时先为所有卡片创建 fallback 分配再抛异常
- [x] `judgeTopicMerge` 强模型单次调用，一次性判断所有新旧主题对
- [x] `judgeTopicMerge` 弱模型分批调用，每批 5 对，含名称相似度 fallback
- [x] `judgeTopicMerge` 合并阈值：语义相似度 > 0.75
- [x] `judgeUnitMerge` 强模型单次调用，一次性判断所有新旧单元对
- [x] `judgeUnitMerge` 弱模型分批调用，每批 5 对，含名称+内容相似度 fallback
- [x] `judgeUnitMerge` 合并阈值：内容相似度 > 0.75
- [x] `loopMergeControl` 最多执行 2 轮
- [x] `loopMergeControl` 终止条件：结构无变化时停止
- [x] `handleTopicConfirm` 不再调用 `classifyCardsByCategoryContent`（new-card-classify 路径）
- [x] `handleTopicConfirm` 不再调用 `mergeSimilarUnits`
- [x] `handleTopicConfirm` 不再执行 `newChapterNameToId` 章节匹配逻辑
- [x] 新管道流程：`generateNewTopicsAndUnits` → `judgeTopicMerge` → `judgeUnitMerge` → `loopMergeControl`
- [x] `mergeSimilarUnits` 已从 Category.jsx 移除 import
- [x] 废弃分类函数 dispatch 保留供其他模式使用（cross-category-auto, same-category-reorganize）
- [ ] 强模型测试：30 个知识点全流程通过
- [ ] 弱模型测试：30 个知识点全流程通过