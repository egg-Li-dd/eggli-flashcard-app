# eggLi 项目文件功能分布记录

## Tailscale VPN 功能相关文件

### Android 原生代码
- `android/app/src/main/java/com/eggli/flashcards/plugins/TailscaleVPNPlugin.kt`
  - 功能：Capacitor 插件，提供 Tailscale VPN 的 JS 桥接接口
  - 主要方法：prepare（VPN 权限准备）、start（启动 Tailscale）、stop（停止）、callLocalAPI（调用本地 API）、getStatus、getLoginURL 等
  - 关键修复：添加了 VpnService.prepare() 权限流程，解决闪退问题

- `android/app/src/main/java/com/eggli/flashcards/plugins/AndroidAppContext.kt`
  - 功能：实现 libtailscale 的 AppContext 接口，提供平台相关能力（存储加密、设备信息、DNS 配置等）

- `android/app/src/main/java/com/eggli/flashcards/services/TailscaleVpnService.kt`
  - 功能：Android VpnService 实现，实现 libtailscale 的 IPNService 接口
  - 关键方法：newBuilder（创建 VPN 构建器）、updateVpnStatus（更新连接状态）、protect（保护 socket）、disconnectVPN（断开 VPN）

### 前端代码
- `src/services/tailscale.js`
  - 功能：前端 Tailscale 服务封装，提供 JS API
  - 主要方法：isAvailable、prepare、start、stop、callLocalAPI、getLoginURL、getStatus 等

### 配置文件
- `android/app/src/main/AndroidManifest.xml`
  - TailscaleVpnService 声明，BIND_VPN_SERVICE 权限
- `android/app/build.gradle`
  - libtailscale.aar 依赖、androidx.security:security-crypto 依赖
- `android/app/libs/libtailscale.aar`
  - Tailscale Android SDK 库文件

### 资源文件
- `android/app/src/main/res/values/strings.xml`
  - tailscale_notification_title、tailscale_notification_text、tailscale_notification_connecting 字符串
- `android/app/src/main/res/drawable/tailscale_ic_launcher.xml`
  - Tailscale 通知图标

## 构建脚本
- `scripts/patch-plugins-json.cjs` - 构建后自动注册自定义插件到 capacitor.plugins.json
- `scripts/build-apk.mjs` - APK 构建脚本

## 系统悬浮窗功能相关文件
- `android/app/src/main/java/com/eggli/flashcards/plugins/SystemFloatingWindowPlugin.kt`
- `android/app/src/main/java/com/eggli/flashcards/services/SystemFloatingWindowService.kt`

## 语音识别相关文件
- `android/app/src/main/java/com/eggli/flashcards/plugins/AudioRecorderPlugin.kt`
- `android/app/src/main/java/com/eggli/flashcards/plugins/VoskASRPlugin.kt`

---

## Android 原生层（native-android）服务与工具文件

### Services（`native-android/app/src/main/java/com/eggli/flashcards/service/`）
- `AiService.kt`
  - 功能：多厂商 AI 大模型统一调用服务（DeepSeek / 讯飞星火 / 火山引擎 / 阿里云百炼）
  - 支持：非流式 chat、流式 chatStream（SSE）、连接测试 testConnection、重试机制
  - 数据类：AiProvider、AiRequest、AiMessage、AiResponse、AiStreamEvent、ConnectionTestResult
- `SpeechRecognitionService.kt`
  - 功能：语音识别双方案封装
  - 方案 A：Android 原生 SpeechRecognizer（免费、离线/在线混合）
  - 方案 B：PC 引擎 WebSocket 代理（管理台 19000 端口，透传 openless）
  - 工具：PcmAudioUtils（Float/Short 转 PCM 16-bit LE）
- `OcrService.kt`
  - 功能：OCR 双方案 + AI 兜底
  - 方案 A：PC 引擎（MinerU / docling）轮询任务获取 Markdown 结果
  - 方案 B：本地 PaddleOCR（预留接口，待集成）
  - 方案 C：AI 多模态大模型兜底识别
  - 工具：ImageProcessingUtils（压缩、Base64）
- `SyncService.kt`
  - 功能：云同步服务（Supabase / 自定义后端）
  - 支持：双向同步 sync、单向推送 push、单向拉取 pull、进度流 syncWithProgress
  - 冲突解决策略：LOCAL_WINS、REMOTE_WINS、TIMESTAMP_WINS、MANUAL
- `TailscaleVpnService.kt`
  - 功能：Tailscale mesh VPN 包装层
  - 核心类：TailscaleVpnServiceImpl（继承 Android VpnService）、TailscaleVpnManager（Hilt 注入包装）
  - 支持：状态流 StateFlow<VpnStatus>、startVpn/stopVpn、本地 API 调用
- `SettingsManager.kt`
  - 功能：加密配置管理（EncryptedSharedPreferences）
  - 存储：API 密钥、Supabase 配置、管理员密码 SHA-256 哈希、用户偏好
  - 辅助：QuickSettings（非敏感配置，普通 SharedPreferences）

### Background Services（`native-android/app/src/main/java/com/eggli/flashcards/service/`）
- `StudyPlanReminderWorker.kt`
  - 功能：WorkManager 周期性 Worker，每日检查到期卡片并推送通知
  - 方法：schedule（注册任务）、cancel（取消）、calculateInitialDelay（计算到下次提醒的延迟）
  - HiltWorker 注入 AppDatabase
- `FloatingWindowService.kt`
  - 功能：全局悬浮窗快速输入服务（前台服务）
  - 支持：拖动、保存快速笔记、最小化、权限检查 canDrawOverlays
  - 需要：SYSTEM_ALERT_WINDOW 权限、TYPE_APPLICATION_OVERLAY

### Utilities（`native-android/app/src/main/java/com/eggli/flashcards/util/`）
- `IdGenerator.kt`
  - UUID v4 生成（标准/紧凑/带业务前缀）、合法性校验
- `TimeUtils.kt`
  - 相对时间格式化（刚刚/X分钟前/X小时前/X天前/yyyy-MM-dd）
  - 日期范围计算（startOfToday/endOfToday/daysAgo/daysLater）
  - 学习时长格式化、星期中文名
- `Sm2Algorithm.kt`
  - SM-2 / 艾宾浩斯遗忘曲线算法实现（与前端完全对齐）
  - 标准 apply、含延迟惩罚 applyWithDelay（onTime/slight/medium/severe）
  - 辅助：isDue、getReviewStatus、estimateDaysToLongTerm、calculateSimpleInterval
- `JsonUtils.kt`
  - AI 响应 JSON 解析工具（5 种降级策略）
  - 安全取值扩展函数（getString/getInt/getBoolean/getArray 等）
  - 损坏 JSON 中提取数组/卡片对象（业务专用）
- `ImageUtils.kt`
  - Bitmap 压缩、缩放、Base64 编解码、旋转、保存文件
  - 内存优化加载（calculateInSampleSize）
- `NetworkUtils.kt`
  - 网络状态检测（isNetworkAvailable/isWifiConnected/isMobileData）
  - IP 地址获取（本地 IP / WiFi 网关）
  - 延迟测试 ping、可达性检查 canReach
- `SecurityUtils.kt`
  - SHA-256 哈希、HMAC-SHA256、管理员密码验证（constant-time 比较防时序攻击）
  - 安全 Token 生成、API 密钥掩码显示 maskApiKey

### Gradle 依赖更新
- `native-android/app/build.gradle` 新增：
  - `androidx.security:security-crypto:1.1.0-alpha06`
  - `androidx.work:work-runtime-ktx:2.9.0`
  - `androidx.hilt:hilt-work:1.1.0` + `ksp 'androidx.hilt:hilt-compiler:1.1.0'`

---

## Compose UI 层相关文件

### 页面（`android/app/src/main/java/com/eggli/flashcards/ui/screen/`）
- `HomeScreen.kt`
  - 功能：记录主页（分类列表 + FAB），对应 React Home.jsx
  - 组件：CategoryCard（分类卡片）、EmptyState（空状态）、CategoryModal（新建/重命名弹窗）、ConfirmDialog（删除确认弹窗）、PurposeModal（编辑学习目的弹窗）、LongPressActionSheet（长按操作菜单）
  - 交互：点击卡片导航到分类详情、长按弹出底部菜单、FAB 新建分类、分类检测按钮导航到单元检测、点击目的标签编辑学习目的

### ViewModel（`android/app/src/main/java/com/eggli/flashcards/ui/viewmodel/`）
- `HomeViewModel.kt`
  - 功能：HomeScreen 的 Hilt ViewModel，管理分类数据和卡片计数
  - 暴露：categories（StateFlow）、cardCounts（StateFlow）
  - 方法：addCategory、renameCategory、deleteCategory、updatePurpose

### 导航（`android/app/src/main/java/com/eggli/flashcards/ui/navigation/`）
- `AppNavHost.kt`
  - 功能：应用导航宿主，Routes.HOME 已接入 HomeScreen 替代占位页面

---

## CloudBase REST API 封装层相关文件

### API 接口层（`android/app/src/main/java/com/eggli/flashcards/data/remote/`）
- `CloudBaseConfig.kt`
  - 功能：CloudBase 环境配置常量（环境 ID、网关 URL、认证端点路径、数据库 REST 路径、EncryptedSharedPreferences 配置、同步表名列表、超时配置）
  - 端点提取来源：@cloudbase/js-sdk 源码（auth 基础路径 `/auth`，端点 `/v1/signin`、`/v1/signup`、`/v1/verification/verify`、`/v1/user/signout`、`/v1/user/me`）
- `CloudBaseApi.kt`
  - 功能：Retrofit 接口定义，包含认证 API（登录、注册、验证码验证、退出、获取用户）和数据库 REST API（查询、插入、更新、删除）
  - 所有数据库操作通过 POST `/v1/rdb/rest/` 端点，通过 action 字段区分操作类型
- `AuthInterceptor.kt`
  - 功能：OkHttp 请求拦截器，自动在请求头添加 `Authorization: Bearer {access_token}`
  - 从 EncryptedSharedPreferences 读取 access_token，登录/注册端点跳过添加 token

### DTO 层（`android/app/src/main/java/com/eggli/flashcards/data/remote/dto/`）
- `AuthDtos.kt`
  - 功能：认证相关请求/响应 DTO（LoginRequest、SignUpRequest、VerifyOtpRequest、UserDto、SessionDto、LoginResponse、SignUpResponse、VerifyOtpResponse、GetUserResponse、CloudBaseResponse、AuthErrorResponse）
  - 字段映射来源：cloudbase.js 的 adaptUser/adaptSession 函数
- `DatabaseDtos.kt`
  - 功能：数据库操作相关 DTO（DbQueryRequest、DbInsertRequest、DbUpdateRequest、DbDeleteRequest 及对应响应 DTO）
  - 包含 DbOrder 排序条件子 DTO

### 仓库层（`android/app/src/main/java/com/eggli/flashcards/data/repository/`）
- `AuthRepository.kt`
  - 功能：认证仓库，封装 CloudBase v3 认证 API 全部操作
  - 主要方法：login（登录）、signUp（注册第一步）、verifyOtp（注册第二步）、logout（退出）、getCurrentUser（获取用户）、isLoggedIn（检查登录状态）、getAccessToken（获取令牌）
  - 会话持久化：使用 EncryptedSharedPreferences 存储 access_token、refresh_token、过期时间、用户信息
  - 工厂方法：create(context) 创建含 Retrofit + OkHttp + AuthInterceptor 的完整实例
  - 错误处理：所有错误消息中文化，对应 cloudbase.js 的 translateSupabaseError 函数
- `SyncRepository.kt`
  - 功能：数据同步仓库，负责本地与云端双向同步
  - 主要方法：pushAll（全量推送）、pullAll（全量拉取）、syncTable（单表同步）、incrementalSync（增量同步）、getLastSyncTime/saveLastSyncTime（同步时间管理）
  - 回调接口：LocalDataProvider（本地数据读取）、LocalDataSaver（本地数据写入），由调用方实现以对接 Room 数据库
  - 分页查询：pullTable 支持分页拉取（每页 100 条）

### Gradle 依赖（`android/app/build.gradle`）
- Retrofit 2.11.0 + converter-gson 2.11.0
- Gson 2.11.0
- OkHttp 4.12.0（含 logging-interceptor）
- androidx.security:security-crypto:1.1.0-alpha06（EncryptedSharedPreferences）
