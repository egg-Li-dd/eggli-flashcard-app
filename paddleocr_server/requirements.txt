# PaddleOCR 自建服务依赖
# 安装命令：pip install -r requirements.txt
#
# 注意：
# - 首次运行会自动下载 PaddleOCR 模型（约 10MB）到 ~/.paddleocr/
# - 如需 GPU 加速，请先安装 paddlepaddle-gpu，然后设置环境变量 USE_GPU=1
# - Python 版本要求：3.8 - 3.11

# PaddlePaddle 框架（CPU 版）
# 如安装失败可参考：https://www.paddlepaddle.org.cn/install/quick
paddlepaddle==2.6.1

# PaddleOCR
paddleocr==2.7.3

# FastAPI Web 框架
fastapi==0.109.0
uvicorn==0.27.0

# 图像处理
Pillow==10.2.0
numpy==1.26.3
